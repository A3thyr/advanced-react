## Сущность валюты

Описание:
содержит набор значений валют и UI-компонент для выбора валюты в пользовательских формах.

#### Public api

- Components

`CurrencySelect` - компонент выбора валюты

- types

`Currency` - enum со списком поддерживаемых валют
